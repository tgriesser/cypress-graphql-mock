{
  "name": "cypress-graphql-mock",
  "description": "Mock out a GraphQL schema from the client",
  "version": "0.1.1",
  "scripts": {
    "dev": "tsc -w",
    "cypress:open": "yarn cypress open --project ./test",
    "cypress:run": "yarn cypress run --project ./test/cypress",
    "run-test-app": "cd ./test/test-graphql-app && SKIP_PREFLIGHT_CHECK=true yarn start",
    "prepublish": "tsc"
  },
  "main": "dist",
  "types": "src",
  "files": [
    "dist/",
    "src/",
    "yarn.lock",
    "README.md"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": {
    "name": "Tim Griesser"
  },
  "dependencies": {
    "graphql": "^14.0.2",
    "graphql-tools": "^4.0.3",
    "tslib": "^1.9.3"
  },
  "peerDependencies": {
    "cypress": "^3.1.3"
  },
  "devDependencies": {
    "@cypress/webpack-preprocessor": "^4.1.0",
    "@types/graphql": "^14.0.3",
    "cypress": "^3.4.1",
    "husky": "^1.2.0",
    "lint-staged": "^8.1.0",
    "prettier": "^1.15.3",
    "ts-loader": "^6.0.4",
    "typescript": "^3.2.2",
    "webpack": "^4.39.2"
  }
}
